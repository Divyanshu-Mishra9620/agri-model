# ---------------------------------------------------------------------------
# KrishiNova Disease Classifier — training pipeline dependencies
#
# IMPORTANT: install PyTorch/torchvision FIRST using the CUDA-specific
# command in README.md ("Installation"), THEN run:
#   pip install -r requirements.txt
# `pip install torch` from PyPI alone gives you a CPU-only build on most
# setups — the README command pins the correct CUDA wheel index instead.
# ---------------------------------------------------------------------------

# Core deep learning
timm>=1.0.11                 # ConvNeXt V2 architectures + pretrained weights
torchmetrics>=1.4.1          # sanity cross-check for confusion-matrix / F1 math

# Data & augmentation
albumentations>=1.4.15
opencv-python-headless>=4.10.0.84
Pillow>=10.4.0
ImageHash>=4.3.1             # perceptual-hash duplicate detection

# Numerics / classical ML
numpy>=1.26.4
pandas>=2.2.2
scikit-learn>=1.5.1

# Config & CLI
PyYAML>=6.0.2

# Experiment tracking / visualization
tensorboard>=2.17.1
matplotlib>=3.9.2

# Export
onnx>=1.16.2
onnxruntime>=1.19.2          # CPU export smoke-test (use onnxruntime-gpu instead if serving on GPU)

# Utilities
tqdm>=4.66.5

# Dev / testing
pytest>=8.3.2
